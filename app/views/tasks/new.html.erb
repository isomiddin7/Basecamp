<nav class="navbar navbar-expand-lg  bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="/"><h4>Hello<%= current_user.email %></h4></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-md-auto gap-2">
        <li class="nav-item rounded">
          <a class="nav-link active" aria-current="page" href="/"><i class="bi bi-house-fill me-2"></i>Home</a>
        </li>

        <li class="nav-item rounded">
         <%= link_to projects_path, class: "nav-link" do %>
            <i class="bi bi-clipboard2-plus-fill"></i> Home Projects
          <% end %>
        </li>
       
      </ul>
    </div>
  </div>
</nav><br />


<%= form_for([@task.project, @task]) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
      <div class="container">
          <div class="container border">
              <div class="container">
                <h4 align="center">To Do App</h4>
                  <label class="form-label" for="form1">Enter a task here</label><br />
                  <div class="input-group mb-3">   
                    <%= f.text_area :description, class: "form-control"  %>
                  </div>
                  <%= f.submit "Creat Task", class:"btn btn-primary"%>
              </div><br />
            </div>
      </div>
            
      <% end %>
    
<div class="container table-responsive py-5"> 
  <table class="table table-bordered table-hover">
    <thead class="text-white bg-dark">
      <tr>
        <th scope="col">id</th>
        <th scope="col">Description</th>
        <th scope="col">Actions</th>
        <th scope="col">Actions</th>
      </tr>
    </thead>
    <tbody>  
        <% @tasks.each do |task| %>
        <% if task.persisted? %>
      <tr>
        <th scope="row"><%= task.id %></th>
        <td><%= task.description %></td>
        <td><%= link_to 'Edit', edit_project_task_path(task.project, task), class: "btn btn-success ms-1"  %></td>
        <td><%= button_to 'Destroy', [task.project, task], method: :delete, class:"btn btn-danger" %></td>
      </tr>  
        <% end %>
        <% end %>
    </tbody>
  </table>
</div>

<style>
.nav-item{
  background-color: white
}
.navbar-nav .nav-item:hover {
  background-color: white;
}
.navbar-brand{
  color: white
}

</style>


