<%= form_for([@attacher.project, @attacher]) do |form| %>
  <% if @attacher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attacher.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @attacher.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="container">
  <div class="mb-2">
          <label for="exampleFormControlInput1" class="form-label">Name</label>
          <%= form.text_field :name, class: "form-control", id: "exampleFormControlInput1", placeholder: "Name" %>
      </div>

    <div class="mb-3">
      <label for="formFileDisabled" class="form-label">Disabled file input example</label>
      <%= form.file_field :uploader, class: "form-control", id: "formFileDisabled" %>

    </div>
     <%= form.submit "Add", class: "btn btn-outline-primary" %> 
</div>
 

   
       
<% end %>